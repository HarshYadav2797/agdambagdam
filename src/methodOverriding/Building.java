package methodOverriding;

public class Building {
	 int x;
	/**
	 * Create a superclass Building with a method build(). Create a subclass House that overrides build(). 
	 * Explain why constructors cannot be overridden by attempting to override the constructor of Building in House.
	 */
public void build()
{
	System.out.println("");
}

}
